[tox]
requires = tox-conda
envlist = py27, py35, py36, py37, py38

[testenv]
deps =
  pytest
  pytest-mock
whitelist_externals =
  bash
commands =
  python -m pytest
  bash -c 'tests/acceptance_test.sh'
